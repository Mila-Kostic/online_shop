"use strict";

var port = 4000; //const SERVER = {

var ServerConfig = {
  port: port,
  //port: 4000, 
  //serverRuningMsg: `Server is runing on port: ${this.port}`
  serverRuningMsg: "Server is runing on port: ".concat(port)
}; //module.exports = SERVER;

module.exports = ServerConfig; //backend setup and login API